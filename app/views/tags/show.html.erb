<% flash.each do %>
  <div id="notice" role="alert" class="alert alert-success"><%= notice %><button type="button" class="close" data-dismiss="alert" aria-label="Close">
    </button></div>
  <% end %>

<div class="tag-styles col-sm-7">
  <% @secondlist.each do |t| %>
    <p class="importance-style"><a href="../seconds/<%= t.id %>"><%= t.name %></a></p>
      <% end %>
</div>
  <%= link_to "back", tags_path(), :class => "red-text tag-item nav-item nav-link" %>
