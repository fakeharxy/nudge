
<p id="notice"><%= notice %></p>


<main role="main" class="col-md-10 ml-sm-auto col-lg-9 pt-3 px-4">

<div class="tag-styles col-sm-7">
  <% @taglist.each do |t| %>
    <p class="importance-style"><%= t.name %></p>
    <div class="btn-group mr-2" role="group" aria-label="First group">
      <% (1..10).each do |n| %>
        <% if n == t.importance %>
          <%= link_to n.to_s, changeimportance_path(tagID:t.id, noteNum:n), class: "btn btn-light active" %>
        <% else %>
          <%= link_to n.to_s, changeimportance_path(tagID:t.id, noteNum:n), class: "btn btn-light" %>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>

<br>

</main>
